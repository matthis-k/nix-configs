local M = {
    ai = require("actions.ai"),
    buffer = require("actions.buffer"),
    debug = require("actions.debug"),
    folds = require("actions.folds"),
    git = require("actions.git"),
    lsp = require("actions.lsp"),
    notifications = require("actions.notifications"),
    snippets = require("actions.snippets"),
    tabs = require("actions.tabs"),
    terminal = require("actions.terminal"),
    types = require("actions.types"),
    ui = require("actions.ui"),
    windows = require("actions.windows"),
}
return M
